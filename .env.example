# Domain configuration
DOMAIN=example.com

# Code-server authentication
CODE_SERVER_PASSWORD=your-secure-password-here

# Authelia secrets (generate each with: openssl rand -hex 32)
AUTHELIA_IDENTITY_VALIDATION_RESET_PASSWORD_JWT_SECRET=generate-64-char-hex-secret
AUTHELIA_SESSION_SECRET=generate-64-char-hex-secret
AUTHELIA_STORAGE_ENCRYPTION_KEY=generate-64-char-hex-secret

# Ollama API key for bearer token auth (generate with: openssl rand -hex 32)
OLLAMA_API_KEY=generate-64-char-hex-api-key
